<h3>{{name}}</h3>
<div class="test-config mdl-card mdl-shadow--2dp">
    <div>
        {{config.url}}
        {{config.browserWidth}}
        {{config.browserHeight}}
        {{config.browser}}
    </div>
</div>
