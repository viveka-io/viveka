<{{tag}} class="mdl-textfield mdl-js-textfield mdl-textfield--floating-label{{#if fullWidth}} mdl-textfield--full-width{{/if}}{{#if tagsInput}} mdlext-tags-input{{/if}}">
    <label class="mdl-textfield__label" for="{{id}}">{{label}}</label>
    <input class="mdl-textfield__input" type="{{type}}" id="{{id}}" value="{{value}}"{{#if maxlength}} maxlength="{{maxlength}}"{{/if}}{{#if min}} min="{{min}}"{{/if}}{{#if tagsInput}} data-role="tagsinput"{{/if}} />
</{{tag}}>
